# Task 3: Simulated Annealing

C++20 implementation of Simulated Annealing for TSP and ATSP. The initial route is generated with Nearest Neighbour and candidate routes are explored using a 2-opt neighbourhood. For small generated instances, the project can calculate an exact Held-Karp reference.

```bash
make
make run-generated
make run-mixed
```
